export default class UTILS {
  constructor() {}

  warn(msg) {
    console.warn("warning => " + msg);
  }

  info(msg) {
    console.log("Info => " + msg);
  }

  error(msg) {
    console.Error("Info => " + msg);
  }
}
